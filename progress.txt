# Ralph Progress Log
Started: Sat May 24 00:00:00     2026




















- **Build**: Passes clean (next build ✓)
- **Notes**: Language switch uses I18nProvider context via useTranslation() hook; no new deps needed (shadcn Select already available)

## P1-21-DateFilter — Date-Match Linked Filtering + Dynamic Data Loading
- **Status**: COMPLETE
- **Build**: Passes clean (next build ✓)
- **Backend Changes**:
  - Added `get_match_dates()` to `MatchRepository` — queries distinct match dates with primary stage (highest precedence wins)
  - Added `get_match_dates()` to `MatchService` — returns `[{date, stage}]`
  - Added `GET /api/matches/dates` endpoint to `match_controller.py`
- **Frontend Changes**:
  - `date-timeline.tsx`: Replaced hardcoded 40-day array with dynamic API fetch via `getMatchDates()`. Auto-selects today or nearest future match date. i18n labels for months/weekdays. Loading skeleton.
  - `match-cards-grid.tsx`: Replaced hardcoded 4-match array with dynamic API fetch via `getMatches({ date })`. Added timezone prop, loading/error/empty states. Added `apiMatchToUi()` converter and `MatchApiItem` type. All text uses `t()`.
  - `page.tsx`: Updated `selectedDate` initial state to empty string (populated by DateTimeline). Passes `timezone` prop to MatchCardsGrid. Footer text uses `t()`.
  - `lib/types/match.ts`: Added `MatchDateInfo` type
  - `lib/api/matches.ts`: Added `getMatchDates()`, `apiMatchToUi()`, `MatchApiItem` type, `inferCityIcon()` helper
  - `lib/i18n/locales/{en-US,zh-CN}.json`: Added `match.noMatches`, `match.today`, `match.errorLoading` keys
  - `lib/i18n/types.ts`: Updated `match` section with 3 new keys
- **Notes**: Date format changed from "Jun 14" to ISO "2026-06-14" for API compatibility. City icon is inferred from venue city name (heuristic). The `common.noMatches` key was already present in en-US but not in match namespace; added match-scoped keys instead.
